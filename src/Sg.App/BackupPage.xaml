<local:SgPage
    x:Class="Sg.App.BackupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sg.App">
    <!-- What the backup repository holds, read before anything is made. A branch is picked from the
         list, its commits and the revisions it was cut from are shown beside what this checkout is at,
         and only then is the button offered - the same shape as importing a file, because it is the
         same question: how far has this checkout moved from where the branch was cut. -->
    <Grid RowDefinitions="Auto,*,Auto,Auto,Auto" RowSpacing="8">

        <InfoBar x:Name="ResultBar" Grid.Row="0" Severity="Success" IsClosable="True" IsOpen="False" Margin="0,0,0,8" />

        <local:EmptyState x:Name="NoBackup" Grid.Row="1" Visibility="Collapsed" Glyph="&#xE74E;" Title="No backup repository is set"
                          Text="Backups go to a git repository somewhere else: a bare folder on a share, or a hosted repository. Set its URL in Settings.">
            <local:EmptyState.Action>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <local:IconButton Glyph="&#xE713;" Text="Settings" Click="Settings_Click" Style="{StaticResource AccentButtonStyle}" />
                    <Button Content="Close" Click="Close_Click" />
                </StackPanel>
            </local:EmptyState.Action>
        </local:EmptyState>

        <local:EmptyState x:Name="Nothing" Grid.Row="1" Visibility="Collapsed" Glyph="&#xE74E;" Title="Nothing of sg's is there yet"
                          Text="The first backup puts every branch there. Press Back up now, or wait for the timer.">
            <local:EmptyState.Action>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <local:IconButton Glyph="&#xE74E;" Text="Back up now" Click="BackupNow_Click" Style="{StaticResource AccentButtonStyle}" />
                    <Button Content="Close" Click="Close_Click" />
                </StackPanel>
            </local:EmptyState.Action>
        </local:EmptyState>

        <ScrollViewer x:Name="Filled" Grid.Row="1" HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Spacing="8" MaxWidth="1000" Padding="0,0,0,8">

                <Border Style="{StaticResource Card}">
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="14">
                        <FontIcon Glyph="&#xE74E;" FontSize="20" VerticalAlignment="Top" Margin="0,2,0,0"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <StackPanel Grid.Column="1" Spacing="2">
                            <TextBlock x:Name="Headline" Style="{StaticResource BodyStrongTextBlockStyle}" TextWrapping="Wrap" />
                            <TextBlock x:Name="UrlText" Style="{StaticResource Secondary}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" />
                        </StackPanel>
                    </Grid>
                </Border>

                <TextBlock x:Name="BranchesHeader" Text="Branches" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,6,0,0" />
                <Border Style="{StaticResource Card}" Padding="4">
                    <local:RowListView x:Name="Branches" SelectionMode="Single" SelectionChanged="Branch_Changed">
                        <local:RowListView.ItemTemplate>
                            <DataTemplate x:DataType="local:BackupRow">
                                <Grid ColumnDefinitions="220,110,*,Auto" ColumnSpacing="{StaticResource RowSpacing}"
                                      ToolTipService.ToolTip="{x:Bind Tip}">
                                    <TextBlock Text="{x:Bind Name}" Style="{StaticResource RowText}" />
                                    <TextBlock Grid.Column="1" Text="{x:Bind What}" Style="{StaticResource RowMonoDim}" />
                                    <TextBlock Grid.Column="2" Text="{x:Bind From}" Style="{StaticResource RowTextDim}" />
                                    <TextBlock Grid.Column="3" Text="{x:Bind Note}" Style="{StaticResource RowTextDim}" Foreground="{x:Bind NoteBrush}" />
                                </Grid>
                            </DataTemplate>
                        </local:RowListView.ItemTemplate>
                    </local:RowListView>
                </Border>

                <InfoBar x:Name="DriftBar" Severity="Warning" IsClosable="False" IsOpen="False"
                         Title="This checkout has moved on since the backup" />

                <TextBlock x:Name="BasesHeader" Text="The revisions it was cut from" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,6,0,0" Visibility="Collapsed" />
                <Border x:Name="BasesCard" Style="{StaticResource Card}" Padding="4" Visibility="Collapsed">
                    <local:RowListView x:Name="Bases" SelectionMode="None">
                        <local:RowListView.ItemTemplate>
                            <DataTemplate x:DataType="local:ImportBaseRow">
                                <Grid ColumnDefinitions="180,90,90,*" ColumnSpacing="{StaticResource RowSpacing}"
                                      ToolTipService.ToolTip="{x:Bind Url}">
                                    <TextBlock Text="{x:Bind Where}" Style="{StaticResource RowText}" />
                                    <TextBlock Grid.Column="1" Text="{x:Bind Exported}" Style="{StaticResource RowMonoDim}" />
                                    <TextBlock Grid.Column="2" Text="{x:Bind Local}" Style="{StaticResource RowMono}" Foreground="{x:Bind LocalBrush}" />
                                    <TextBlock Grid.Column="3" Text="{x:Bind Shown}" Style="{StaticResource RowTextDim}" />
                                </Grid>
                            </DataTemplate>
                        </local:RowListView.ItemTemplate>
                    </local:RowListView>
                </Border>

                <TextBlock x:Name="CommitsHeader" Text="Commits" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,6,0,0" Visibility="Collapsed" />
                <Border x:Name="CommitsCard" Style="{StaticResource Card}" Padding="4" Visibility="Collapsed">
                    <local:RowListView x:Name="Subjects" SelectionMode="None">
                        <local:RowListView.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <TextBlock Text="{x:Bind}" Style="{StaticResource RowText}" />
                            </DataTemplate>
                        </local:RowListView.ItemTemplate>
                    </local:RowListView>
                </Border>

                <TextBlock x:Name="OthersHeader" Text="Also there" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="0,6,0,0" Visibility="Collapsed" />
                <Border x:Name="OthersCard" Style="{StaticResource Card}" Padding="4" Visibility="Collapsed">
                    <local:RowListView x:Name="Others" SelectionMode="None">
                        <local:RowListView.ItemTemplate>
                            <DataTemplate x:DataType="local:BackupRow">
                                <Grid ColumnDefinitions="220,*,Auto" ColumnSpacing="{StaticResource RowSpacing}" ToolTipService.ToolTip="{x:Bind Tip}">
                                    <TextBlock Text="{x:Bind Name}" Style="{StaticResource RowText}" />
                                    <TextBlock Grid.Column="1" Text="{x:Bind What}" Style="{StaticResource RowTextDim}" />
                                    <TextBlock Grid.Column="2" Text="{x:Bind Note}" Style="{StaticResource RowTextDim}" Foreground="{x:Bind NoteBrush}" />
                                </Grid>
                            </DataTemplate>
                        </local:RowListView.ItemTemplate>
                    </local:RowListView>
                </Border>

                <TextBlock x:Name="ConflictsHeader" Text="Files that need a hand" Style="{StaticResource BodyStrongTextBlockStyle}"
                           Margin="0,6,0,0" Visibility="Collapsed" />
                <Border x:Name="ConflictsCard" Style="{StaticResource Card}" Padding="4" Visibility="Collapsed">
                    <local:RowListView x:Name="Conflicts" SelectionMode="None">
                        <local:RowListView.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <TextBlock Text="{x:Bind}" Style="{StaticResource RowMono}" />
                            </DataTemplate>
                        </local:RowListView.ItemTemplate>
                    </local:RowListView>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Grid x:Name="RestoreRow" Grid.Row="2" ColumnDefinitions="Auto,*,Auto,220,Auto" ColumnSpacing="8" Margin="0,4,0,0">
            <TextBlock Text="As branch" Style="{StaticResource BodyStrongTextBlockStyle}" VerticalAlignment="Center" />
            <TextBox x:Name="NameBox" Grid.Column="1" TextChanged="Name_TextChanged" MaxWidth="320" HorizontalAlignment="Left"
                     ToolTipService.ToolTip="The name the branch gets here. It starts as the name it has in the backup, which is free to be something else." />
            <TextBlock Grid.Column="2" Text="Into checkout" Style="{StaticResource BodyStrongTextBlockStyle}" VerticalAlignment="Center" />
            <ComboBox x:Name="IntoBox" Grid.Column="3" HorizontalAlignment="Stretch" SelectionChanged="Into_Changed"
                      ToolTipService.ToolTip="The checkout the branch is built on. It should be one of the same SVN repository the backup names." />
            <StackPanel Grid.Column="4" Orientation="Vertical" VerticalAlignment="Center">
                <CheckBox x:Name="WipBox" Content="With its uncommitted changes" IsChecked="True"
                          ToolTipService.ToolTip="The changes that were not committed when the backup was made come back too, through the shelf: written into the worktree when they merge, left on the shelf when one does not." />
                <CheckBox x:Name="ForceBox" Content="Overwrite if it exists here" Checked="Force_Changed" Unchecked="Force_Changed"
                          ToolTipService.ToolTip="A branch of this name already here is written over with the backup version - its worktree reset to it, any uncommitted changes in it dropped. Off, restore refuses a name that is taken." />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="3" ColumnDefinitions="Auto,Auto,Auto,*,Auto" ColumnSpacing="8" Margin="0,4,0,0">
            <local:IconButton Glyph="&#xE74E;" Text="Back up now" Click="BackupNow_Click" x:Name="BackupNowButton"
                              ToolTipService.ToolTip="Send every branch, the uncommitted changes and the shelves to the backup repository now, rather than waiting for the timer." />
            <local:IconButton Grid.Column="1" Glyph="&#xE74D;" Text="Prune" Click="Prune_Click" x:Name="PruneButton"
                              ToolTipService.ToolTip="Delete from the backup what is not here any more: branches removed, shelves dropped. Asks first, with the list." />
            <Button Grid.Column="2" Content="Close" Click="Close_Click" ToolTipService.ToolTip="Back to where you came from. Nothing is written by leaving." />
            <TextBlock Grid.Column="3" x:Name="Summary" Style="{StaticResource Secondary}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
            <Button Grid.Column="4" x:Name="RestoreButton" Style="{StaticResource AccentButtonStyle}" Click="Restore_Click"
                    AutomationProperties.Name="Restore the branch"
                    ToolTipService.ToolTip="Make the branch and its worktree here, then merge every commit onto this checkout's snapshot.">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE896;" FontSize="14" />
                    <TextBlock x:Name="RestoreLabel" Text="Restore" />
                </StackPanel>
            </Button>
        </Grid>

        <local:StatusStrip x:Name="Pane" Grid.Row="4" Margin="0,8,0,0" />
    </Grid>
</local:SgPage>
